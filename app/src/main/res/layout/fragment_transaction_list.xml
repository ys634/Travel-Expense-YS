<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".TransactionListFragment">


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/transactionListRecView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/floatingBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="20dp"
        app:backgroundTint="@color/topaz"
        android:elevation="6dp"
        android:src="@drawable/ic_add"/>

    <TextView
        android:id="@+id/txtNoTransaction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:layout_marginTop="15dp"
        android:padding="5dp"
        android:visibility="gone"
        android:text="No transaction recorded. \nAdd your first one by tapping the button on the bottom!"/>


</FrameLayout>